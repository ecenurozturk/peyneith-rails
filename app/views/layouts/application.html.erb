<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <div class="top-bar-title">
        <%= link_to image_tag("logo.jpg"), root_path %>
      </div>
      <div id="responsive-menu">
        <div class="top-bar-left">
          <ul class="dropdown menu" data-dropdown-menu>
            <li><%= link_to "Homeworks", homeworks_path %></li>
            <li><%= link_to "Lessons", lessons_path %></li>
          </ul>
        </div>
        <% if  user_signed_in? %>
        <div class="top-bar-right">
          <ul class="menu">
            <li><%= link_to "My Profile",edit_user_registration_path(current_user), {:class => 'button'} %></li>
            <li>|</li>
            <li><%= link_to "Log Out",destroy_user_session_path, {:class => 'button', :method => 'DELETE'} %></li>
          </ul>
        </div>
        <% end %>
      </div>
    </div>

    <%= yield %>

  </body>
</html>
