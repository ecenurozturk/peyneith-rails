<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <div class="top-bar-title">
        <%= link_to image_tag("logo.jpg"), root_path %>
      </div>
      <div id="responsive-menu">
        <div class="top-bar-left">
          <% if user_signed_in? %>
            <% if current_user.is_admin == 1 %>
              <ul class="menu">
                <li><%= link_to "Homeworks", homeworks_path, {:class => 'success button  '} %></li>
                <li>&nbsp;</li>
                <li><%= link_to "Lessons", lessons_path, {:class => 'success button  '} %></li>
                <li>&nbsp;</li>
                <li><%= link_to "Students", students_path,  {:class => 'success button  '} %></li>
              </ul>
            <% end %>
          <% end %>
        </div>
        <div class="top-bar-right">
          <ul class="menu">
            <% if  user_signed_in? %>
              <% if current_user.is_admin? %>
                <li><%= link_to "Admin Panel", rails_admin.dashboard_path, {:class => 'button alert'} %></li>
                <li>&nbsp;</li>
              <% end %>
              <li><%= link_to "Log Out",destroy_user_session_path, {:class => 'button', :method => 'DELETE'} %></li>
            <% else %>
              <li><%= link_to "Sign In", new_user_session_path, {:class => 'button'} %></li>
            <% end %>
            <li>&nbsp;</li>
          </ul>
        </div>
      </div>
    </div>

    <div class="row">
      <p id="notice"><%= notice %></p>
      <% if user_signed_in? %>
        <!-- Sidebar -->
              <div class="large-2 columns sidebar">

                  <ul class="menu vertical">
                    <li><%= link_to "Home", root_path, {:class => 'success button'} %></li>
                    <li>&nbsp;</li>
                    <li><%= link_to "My Profile", edit_user_registration_path(current_user), {:class => 'success button'} %></li>
                    <% if current_user.is_teacher == 1 %>
                      <li>&nbsp;</li>
                      <li><%= link_to "New Homework", new_homework_path, {:class => 'success button'} %></li>
                    <% end %>
                  </ul>

              </div>
            <!-- End Sidebar -->
        <% end %>
      <div class="large-9 columns">
        <%= yield %>
        <br>
      </div>

    </div>

    <footer class="footer">
      <div class="row column">
     <hr>
     <ul class="menu">
        <li>Peyneith Homework System</li>
        <li><%= link_to 'Home', root_path %></li>
        <li><%= link_to 'About' %></li>
        <li><%= link_to 'Contact'%></li>
        <li class="float-right">Copyright 2016</li>
     </ul>
  </div>
  </footer>
  </body>
</html>
